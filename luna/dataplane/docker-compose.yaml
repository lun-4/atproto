services:
  dataplane_db:
    image: postgres:17
    ports:
      - '5858:5432'
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    healthcheck:
      disable: true
    volumes:
      - ./dataplane_db_data:/var/lib/postgresql/data
